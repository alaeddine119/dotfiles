# Main Entry Point — Modular Sway Config
# Order matters: variables and hardware maps must load before bindsyms loop!

include config.d/*

# Keep global system overrides active
include /etc/sway/config.d/*
